const source = {
	description: 'Default command executed when the requested command is not found.',
	reply: 'Command "{0}" not found. Please notify an administrator.',
};

export default source;
export type Messages = typeof source;
